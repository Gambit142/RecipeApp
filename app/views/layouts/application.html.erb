<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/29d7bf5b00.js" crossorigin="anonymous"></script>
  </head>

  <body>

    <header class="header">
      <div class="links">
        <%= link_to "Foods", foods_url %>
        <%= link_to "Recipes", recipes_url %>
        <%= link_to "Shopping List", shopping_list_url %>
        <%= link_to "Public Recipes", public_recipes_url %>
      </div>
      <div>
        <% if user_signed_in? %>
          <span class="logged-in">
            <i class="fas fa-user-circle"></i>
            <span><%= current_user.name.capitalize %></span>
          </span>
          <%= link_to 'Sign Out', destroy_user_session_url, method: :delete, class: 'sign button' %>
        <% else %>
          <%= link_to 'Sign In / Sign Up', new_user_session_url, class: 'sign button' %>
        <% end %>
      </div>
    </header>

    <div>
      <% flash.each do |name, msg| -%>
        <%= content_tag :span, msg, class: name %>
      <% end -%>
    </div>
    <%= yield %>
  </body>
</html>
